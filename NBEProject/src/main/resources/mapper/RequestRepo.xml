<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lec.spring.repository.RequestRepo">
    <insert id="insert" flushCache="true" parameterType="com.lec.spring.domain.shop.Request">
        insert into nbe_db.request (user_id, item_id, type)
        value (#{user_id}, #{item_id}, #{type})
    </insert>

    <delete id="delete" flushCache="true">
        delete from nbe_db.request
        where item_id=#{item_id} and user_id=#{user_id}
    </delete>
    <update id="update" flushCache="true" parameterType="com.lec.spring.domain.shop.Request">
        update nbe_db.request
        set result=#{result}
        where item_id=#{item_id} and user_id=#{user_id}
    </update>

</mapper>